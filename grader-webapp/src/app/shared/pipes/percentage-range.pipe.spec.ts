import { PercentageRangePipe } from './percentage-range.pipe';

describe('PercentageRangePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageRangePipe();
    expect(pipe).toBeTruthy();
  });
});
